const colors = {
    primary: '#365E32',
    secondary: '#81A263',
    tertiary: '#E7D37F',
    quaternary: '#FD9B63',
    white: '#fff',
    black: '#333333',
    grey: '#757575'
};

const fontSizes = {
    body: 16,
    subheading: 18,
    title: 32,
};

const fontWeights = {
    normal: '400',
    bold: '700',
};

const alignments = {
    center: 'center',
    left: 'left',
    right: 'right',
    justify: 'justify',
};

const theme = {
    colors: {
        ...colors,
    },
    fontSizes: {
        ...fontSizes,
    },
    fontWeights: {
        ...fontWeights,
    },
    alignments: {
        ...alignments,
    }
};

export default theme;
